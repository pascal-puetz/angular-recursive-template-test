import { MapStringsToTextEntryPipe } from './map-strings-to-text-entry.pipe';

describe('MapStringsToTextEntryPipe', () => {
  it('create an instance', () => {
    const pipe = new MapStringsToTextEntryPipe();
    expect(pipe).toBeTruthy();
  });
});
